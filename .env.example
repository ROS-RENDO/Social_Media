# ============================================
# Docker & Database Configuration
# ============================================

# MySQL Root Password (REQUIRED - for docker-compose)
MYSQL_ROOT_PASSWORD=rootpassword

# MySQL Database Name (REQUIRED - for docker-compose)
MYSQL_DATABASE=social_media

# MySQL User (REQUIRED - for docker-compose)
MYSQL_USER=socialuser

# MySQL User Password (REQUIRED - for docker-compose)
MYSQL_PASSWORD=userpassword

# ============================================
# Backend Configuration
# ============================================

# Node.js Environment
NODE_ENV=development

# Server Port
PORT=3001

# Frontend URL (where frontend is accessible)
FRONTEND_URL=http://localhost:3000

# JWT Secret (for authentication tokens - use a strong random string in production)
JWT_SECRET=your-jwt-secret-key-change-this-in-production-min-32-chars

# Better Auth Secret (for session management - use a strong random string in production)
BETTER_AUTH_SECRET=your-better-auth-secret-change-this-in-production-min-32-chars

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL (visible to browser)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Better Auth URL (visible to browser)
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:3001

# ============================================
# PRODUCTION NOTES
# ============================================
# For production deployment:
# 1. Change MYSQL_ROOT_PASSWORD to a strong password (20+ characters)
# 2. Change MYSQL_PASSWORD to a strong password (20+ characters)
# 3. Change JWT_SECRET to a strong random string (32+ characters)
# 4. Change BETTER_AUTH_SECRET to a strong random string (32+ characters)
# 5. Update FRONTEND_URL to your production domain (https://yourdomain.com)
# 6. Update NEXT_PUBLIC_API_URL to your production API URL (https://yourdomain.com/api)
# 7. Update NEXT_PUBLIC_BETTER_AUTH_URL to your production auth URL (https://yourdomain.com)
